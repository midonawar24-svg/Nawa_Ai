/// V12 Constants
class AppConstants {
  static const String appName = 'AI CORE OS';
  static const String version = '12.0.0';
  static const String neuralLinkActive = 'Neural Link Active';
  
  // Colors
  static const int deepBlack = 0xFF050507;
  static const int cyanNeon = 0xFF00D9FF;
  static const int purpleNeon = 0xFF7C3AED;
  
  // Durations
  static const Duration bootDuration = Duration(seconds: 3);
  static const Duration fadeDuration = Duration(milliseconds: 500);
  static const Duration slideDuration = Duration(milliseconds: 300);
}
